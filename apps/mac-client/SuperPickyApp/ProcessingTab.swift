import SwiftUI

struct ProcessingTab: View {
    var body: some View {
        Form {
            Text("Processing settings will appear here.")
                .foregroundStyle(.secondary)
        }
        .formStyle(.grouped)
    }
}
